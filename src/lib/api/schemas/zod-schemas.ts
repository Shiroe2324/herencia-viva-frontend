/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Herencia Viva API Documentation
 * Complete REST API for authentication, users, clients and bots. Includes JWT authentication, Google OAuth2, MFA/2FA, role-based authorization, file uploads, background processing, and an AI chatbot recommendation module fine-tuned for the agricultural sector.
 *
 * ---
 *
 * ## Global Error Codes
 *
 * ---
 *
 * All API endpoints that return an error status code (4xx or 5xx) return a response body that includes an `code` field. This code allows the client to handle errors programmatically. Below are all possible codes:
 *
 * ---
 *
 * ### Authentication Error Codes (AUTH)
 *
 * - `AUTH.TOKEN_INVALID`
 * - `AUTH.TOKEN_EXPIRED`
 * - `AUTH.TOKEN_NOT_FOUND`
 * - `AUTH.TOKENS_ALREADY_LINKED`
 * - `AUTH.PASSWORD_INVALID`
 * - `AUTH.PASSWORD_REQUIRED`
 * - `AUTH.PASSWORD_FORBIDDEN`
 * - `AUTH.PASSWORD_RESET_PENDING`
 * - `AUTH.PASSWORD_SAME_AS_CURRENT`
 * - `AUTH.MFA_TOKEN_INVALID`
 * - `AUTH.MFA_REQUIRED`
 * - `AUTH.MFA_ALREADY_ENABLED`
 * - `AUTH.MFA_REGENERATION_RATE_LIMITED`
 * - `AUTH.ROLE_INSUFFICIENT`
 * - `AUTH.ROLE_NOT_FOUND`
 * - `AUTH.CLIENT_NOT_FOUND`
 * - `AUTH.USER_NOT_FOUND`
 * - `AUTH.USER_EXTERNAL_REQUIRED`
 * - `AUTH.USER_ALREADY_EXISTS`
 * - `AUTH.EMAIL_MISSING`
 * - `AUTH.EMAIL_ALREADY_IN_USE`
 * - `AUTH.EMAIL_NOT_VERIFIED`
 * - `AUTH.GOOGLE_TOKEN_INVALID`
 * - `AUTH.APPLE_TOKEN_INVALID`
 * - `AUTH.OTP_SESSION_INVALID`
 * - `AUTH.SESSION_LOG_NOT_FOUND`
 *
 * ---
 *
 * ### Recommendations Error Codes
 *
 * - `RECOMMENDATIONS.CHAT_NOT_FOUND`
 * - `RECOMMENDATIONS.CHAT_NO_CHANGES`
 * - `RECOMMENDATIONS.AI_GENERATION_FAILED`
 * - `RECOMMENDATIONS.CONTEXT_NOT_FOUND`
 * - `RECOMMENDATIONS.CONTEXT_NO_CHANGES`
 *
 * ---
 *
 * ### Users Error Codes (USERS)
 *
 * - `USERS.CLIENT_ALREADY_EXISTS`
 * - `USERS.CLIENT_NOT_FOUND`
 * - `USERS.CLIENT_NO_CHANGES`
 * - `USERS.USER_NOT_FOUND`
 * - `USERS.USER_NOT_DELETED`
 * - `USERS.USER_NO_CHANGES`
 * - `USERS.USERNAME_ALREADY_IN_USE`
 * - `USERS.PROFILE_PICTURE_NOT_FOUND`
 * - `USERS.PROFILE_PICTURE_PROCESSING`
 * - `USERS.RECOVERY_TOKEN_ALREADY_SENT`
 * - `USERS.RECOVERY_TOKEN_NOT_FOUND`
 * - `USERS.RECOVERY_TOKEN_EXPIRED`
 *
 * ---
 *
 * ### Generic Error Codes
 *
 * - `GENERIC.INTERNAL_SERVER_ERROR`
 * - `GENERIC.INVALID_INPUT`
 * - `GENERIC.THROTTLED`
 * - `GENERIC.NOT_FOUND`
 * OpenAPI spec version: 0.9.1
 */
import * as zod from 'zod';

/**
 * Check the health status of the application
 * @summary Health Check
 */
export const healthCoreControllerCheckHeaderXLangDefault = `es`;

export const HealthCoreControllerCheckHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(healthCoreControllerCheckHeaderXLangDefault).describe('Language code'),
});

export const HealthCoreControllerCheckResponse = zod.object({
  status: zod.string().optional(),
  info: zod
    .record(
      zod.string(),
      zod.object({
        status: zod.string(),
      }),
    )
    .nullish(),
  error: zod
    .record(
      zod.string(),
      zod.object({
        status: zod.string(),
      }),
    )
    .nullish(),
  details: zod
    .record(
      zod.string(),
      zod.object({
        status: zod.string(),
      }),
    )
    .optional(),
});

/**
 * Provides a real-time SSE stream of BullMQ job lifecycle events for queued background work, including active, progress, completed, failed, stalled, and error notifications.
 * @summary Stream job notifications
 */
export const StreamJobNotificationsQueryParams = zod.object({
  queue: zod.enum(['SEND_MAIL', 'UPDATE_IMAGE', 'DELETE_IMAGE']).describe('BullMQ queue name that created the job.'),
  jobId: zod.string().describe('BullMQ job identifier to follow.'),
});

export const streamJobNotificationsHeaderXLangDefault = `es`;

export const StreamJobNotificationsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(streamJobNotificationsHeaderXLangDefault).describe('Language code'),
});

export const StreamJobNotificationsResponse = zod.unknown();

/**
 * Uses Gemini + Chroma retrieval to generate persistent chat responses for cattle farmers while storing the conversation history in the database.
 * @summary Generate Cattle Farming Recommendation
 */
export const askRecommendationHeaderXLangDefault = `es`;

export const AskRecommendationHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(askRecommendationHeaderXLangDefault).describe('Language code'),
});

export const askRecommendationBodyQuestionMin = 5;
export const askRecommendationBodyQuestionMax = 1000;

export const askRecommendationBodyLimitDefault = 10;
export const askRecommendationBodyLimitMax = 10;

export const AskRecommendationBody = zod
  .object({
    question: zod
      .string()
      .min(askRecommendationBodyQuestionMin)
      .max(askRecommendationBodyQuestionMax)
      .describe('Question from the farmer or advisor about livestock, feeding, pasture, health, productivity, or management.'),
    limit: zod
      .number()
      .min(1)
      .max(askRecommendationBodyLimitMax)
      .default(askRecommendationBodyLimitDefault)
      .describe('Maximum number of similar Chroma chunks used as context (between 1 and 10).'),
    chat_id: zod.string().nullish().describe('Optional chat identifier used to continue a persistent conversation.'),
  })
  .describe('Contains the user question, optional retrieval depth, and an optional chat identifier to continue a conversation.');

export const AskRecommendationResponse = zod
  .object({
    chat_id: zod.string().describe('Persistent chat identifier stored in the database.'),
    answer: zod.string().describe('Final recommendation generated by Gemini grounded on the retrieved context.'),
    contexts: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique identifier of the retrieved record in Chroma.'),
            question: zod.string().optional().describe('Original question text stored in record metadata when available.'),
            answer: zod.string().optional().describe('Original answer text stored in record metadata when available.'),
            tags: zod.array(zod.string()).nullish().describe('Optional tags stored in record metadata when available.'),
            document: zod.string().optional().describe('Raw retrieved document as stored in Chroma.'),
            distance: zod.number().optional().describe('Similarity distance returned by Chroma (lower is usually closer).'),
          })
          .describe(
            'Represents a single context chunk retrieved from ChromaDB, which may include the original question, answer, source document, and similarity distance.',
          ),
      )
      .describe('Retrieved context entries returned by Chroma and used in the prompt.'),
    messages: zod
      .array(
        zod
          .object({
            role: zod.enum(['user', 'system', 'assistant']).describe('Message author within the conversation, either user or assistant.'),
            content: zod.string().describe('Message content saved in the database.'),
            created_at: zod.string().describe('ISO timestamp indicating when the message was stored.'),
          })
          .describe('Represents a single persisted chat message within a recommendation conversation.'),
      )
      .describe('Ordered chat messages persisted for the current conversation.'),
  })
  .describe('Contains the AI recommendation, the retrieved context, and the persisted chat history used to build it.');

/**
 * @summary Stream Cattle Farming Recommendation
 */
export const recommendationsLLMControllerAskChatStreamQueryQuestionMin = 5;
export const recommendationsLLMControllerAskChatStreamQueryQuestionMax = 1000;

export const recommendationsLLMControllerAskChatStreamQueryLimitDefault = 10;
export const recommendationsLLMControllerAskChatStreamQueryLimitMax = 10;

export const RecommendationsLLMControllerAskChatStreamQueryParams = zod.object({
  question: zod
    .string()
    .min(recommendationsLLMControllerAskChatStreamQueryQuestionMin)
    .max(recommendationsLLMControllerAskChatStreamQueryQuestionMax)
    .describe('Question from the farmer or advisor about livestock, feeding, pasture, health, productivity, or management.'),
  limit: zod
    .number()
    .min(1)
    .max(recommendationsLLMControllerAskChatStreamQueryLimitMax)
    .default(recommendationsLLMControllerAskChatStreamQueryLimitDefault)
    .describe('Maximum number of similar Chroma chunks used as context (between 1 and 10).'),
  chatId: zod.string().nullish().describe('Optional chat identifier used to continue a persistent conversation.'),
});

export const recommendationsLLMControllerAskChatStreamHeaderXLangDefault = `es`;

export const RecommendationsLLMControllerAskChatStreamHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(recommendationsLLMControllerAskChatStreamHeaderXLangDefault).describe('Language code'),
});

export const RecommendationsLLMControllerAskChatStreamResponse = zod.unknown();

/**
 * Retrieves a paginated list of recommendation chats that belong to the authenticated user.
 * @summary List Recommendation Chats
 */
export const getAllRecommendationChatsQueryOrderDirectionDefault = `DESC`;
export const getAllRecommendationChatsQueryOrderByDefault = `updatedAt`;
export const getAllRecommendationChatsQueryPageDefault = 1;

export const getAllRecommendationChatsQueryLimitDefault = 10;
export const getAllRecommendationChatsQueryLimitMax = 100;

export const GetAllRecommendationChatsQueryParams = zod.object({
  orderDirection: zod.enum(['ASC', 'DESC']).default(getAllRecommendationChatsQueryOrderDirectionDefault).describe('Sorting direction (ASC or DESC).'),
  orderBy: zod
    .enum(['id', 'title', 'createdAt', 'updatedAt'])
    .default(getAllRecommendationChatsQueryOrderByDefault)
    .describe('Field used to sort chats (createdAt, updatedAt, or title).'),
  page: zod.number().min(1).default(getAllRecommendationChatsQueryPageDefault).describe('Page number for pagination (starts at 1).'),
  limit: zod
    .number()
    .min(1)
    .max(getAllRecommendationChatsQueryLimitMax)
    .default(getAllRecommendationChatsQueryLimitDefault)
    .describe('Number of chats returned per page.'),
});

export const getAllRecommendationChatsHeaderXLangDefault = `es`;

export const GetAllRecommendationChatsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getAllRecommendationChatsHeaderXLangDefault).describe('Language code'),
});

export const GetAllRecommendationChatsResponse = zod
  .object({
    total: zod.number().describe('Total number of recommendation chats owned by the authenticated user.'),
    page: zod.number().describe('Current page number.'),
    limit: zod.number().describe('Number of chats per page.'),
    total_pages: zod.number().describe('Total number of pages available.'),
    chats: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique recommendation chat identifier (UUID format)'),
            title: zod.looseObject({}).nullable().describe('Optional title generated from the first user message'),
            messages: zod
              .array(
                zod
                  .object({
                    id: zod.string().describe('Unique recommendation chat message identifier (UUID format)'),
                    role: zod.enum(['user', 'system', 'assistant']).describe('Message author role in the conversation (user or model)'),
                    content: zod.string().describe('Message text content stored in the database'),
                    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp when the message was created'),
                    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last message update'),
                  })
                  .describe('Represents a single message inside a recommendation chat session'),
              )
              .describe('Ordered list of persisted chat message entities between user and assistant'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of chat creation'),
            updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last chat update'),
          })
          .describe('Represents a persistent AI recommendation chat associated with a user'),
      )
      .describe('Array of recommendation chats.'),
  })
  .describe('Paginated recommendation chat list with metadata.');

/**
 * Retrieves a single recommendation chat by identifier when it belongs to the authenticated user.
 * @summary Get Recommendation Chat
 */
export const GetRecommendationChatParams = zod.object({
  chatId: zod.string().describe('Recommendation chat identifier (UUID).'),
});

export const getRecommendationChatHeaderXLangDefault = `es`;

export const GetRecommendationChatHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getRecommendationChatHeaderXLangDefault).describe('Language code'),
});

export const GetRecommendationChatResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation chat identifier (UUID format)'),
    title: zod.looseObject({}).nullable().describe('Optional title generated from the first user message'),
    messages: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique recommendation chat message identifier (UUID format)'),
            role: zod.enum(['user', 'system', 'assistant']).describe('Message author role in the conversation (user or model)'),
            content: zod.string().describe('Message text content stored in the database'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp when the message was created'),
            updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last message update'),
          })
          .describe('Represents a single message inside a recommendation chat session'),
      )
      .describe('Ordered list of persisted chat message entities between user and assistant'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of chat creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last chat update'),
  })
  .describe('Represents a persistent AI recommendation chat associated with a user');

/**
 * Updates editable fields of a recommendation chat owned by the authenticated user.
 * @summary Update Recommendation Chat
 */
export const PatchRecommendationChatParams = zod.object({
  chatId: zod.string().describe('Recommendation chat identifier (UUID).'),
});

export const patchRecommendationChatHeaderXLangDefault = `es`;

export const PatchRecommendationChatHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(patchRecommendationChatHeaderXLangDefault).describe('Language code'),
});

export const patchRecommendationChatBodyTitleMin = 3;
export const patchRecommendationChatBodyTitleMax = 120;

export const PatchRecommendationChatBody = zod
  .object({
    title: zod
      .string()
      .min(patchRecommendationChatBodyTitleMin)
      .max(patchRecommendationChatBodyTitleMax)
      .optional()
      .describe('New chat title within the configured length limits.'),
  })
  .describe('Editable fields of a recommendation chat.');

export const PatchRecommendationChatResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation chat identifier (UUID format)'),
    title: zod.looseObject({}).nullable().describe('Optional title generated from the first user message'),
    messages: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique recommendation chat message identifier (UUID format)'),
            role: zod.enum(['user', 'system', 'assistant']).describe('Message author role in the conversation (user or model)'),
            content: zod.string().describe('Message text content stored in the database'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp when the message was created'),
            updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last message update'),
          })
          .describe('Represents a single message inside a recommendation chat session'),
      )
      .describe('Ordered list of persisted chat message entities between user and assistant'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of chat creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last chat update'),
  })
  .describe('Represents a persistent AI recommendation chat associated with a user');

/**
 * Soft deletes a recommendation chat that belongs to the authenticated user.
 * @summary Delete Recommendation Chat
 */
export const DeleteRecommendationChatParams = zod.object({
  chatId: zod.string().describe('Recommendation chat identifier (UUID).'),
});

export const deleteRecommendationChatHeaderXLangDefault = `es`;

export const DeleteRecommendationChatHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(deleteRecommendationChatHeaderXLangDefault).describe('Language code'),
});

export const DeleteRecommendationChatResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation chat identifier (UUID format)'),
    title: zod.looseObject({}).nullable().describe('Optional title generated from the first user message'),
    messages: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique recommendation chat message identifier (UUID format)'),
            role: zod.enum(['user', 'system', 'assistant']).describe('Message author role in the conversation (user or model)'),
            content: zod.string().describe('Message text content stored in the database'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp when the message was created'),
            updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last message update'),
          })
          .describe('Represents a single message inside a recommendation chat session'),
      )
      .describe('Ordered list of persisted chat message entities between user and assistant'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of chat creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last chat update'),
  })
  .describe('Represents a persistent AI recommendation chat associated with a user');

/**
 * Retrieves a paginated list of recommendation contexts managed by administrators.
 * @summary List Recommendation Contexts
 */
export const getAllRecommendationContextsQueryOrderByDefault = `updatedAt`;
export const getAllRecommendationContextsQueryOrderDirectionDefault = `DESC`;
export const getAllRecommendationContextsQueryPageDefault = 1;

export const getAllRecommendationContextsQueryLimitDefault = 10;
export const getAllRecommendationContextsQueryLimitMax = 100;

export const GetAllRecommendationContextsQueryParams = zod.object({
  orderBy: zod
    .enum(['id', 'question', 'answer', 'createdAt', 'updatedAt'])
    .default(getAllRecommendationContextsQueryOrderByDefault)
    .describe('Field used to sort contexts (createdAt, updatedAt, question, or answer).'),
  orderDirection: zod
    .enum(['ASC', 'DESC'])
    .default(getAllRecommendationContextsQueryOrderDirectionDefault)
    .describe('Sorting direction (ASC or DESC).'),
  page: zod.number().min(1).default(getAllRecommendationContextsQueryPageDefault).describe('Page number for pagination (starts at 1).'),
  limit: zod
    .number()
    .min(1)
    .max(getAllRecommendationContextsQueryLimitMax)
    .default(getAllRecommendationContextsQueryLimitDefault)
    .describe('Number of contexts returned per page.'),
});

export const getAllRecommendationContextsHeaderXLangDefault = `es`;

export const GetAllRecommendationContextsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getAllRecommendationContextsHeaderXLangDefault).describe('Language code'),
});

export const GetAllRecommendationContextsResponse = zod
  .object({
    total: zod.number().describe('Total number of recommendation contexts.'),
    page: zod.number().describe('Current page number.'),
    limit: zod.number().describe('Number of contexts per page.'),
    total_pages: zod.number().describe('Total number of pages available.'),
    contexts: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique recommendation context identifier (UUID format)'),
            question: zod.string().describe('Context question used to build the vector search record'),
            answer: zod.string().describe('Context answer associated with the question'),
            tags: zod.array(zod.string()).nullable().describe('Optional tags for categorizing or filtering recommendation contexts'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of context creation'),
            updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last context update'),
          })
          .describe('Represents a recommendation context mirrored from the vector database for fast relational access'),
      )
      .describe('Array of recommendation contexts.'),
  })
  .describe('Paginated recommendation context list with metadata.');

/**
 * Creates a recommendation context and synchronizes it into the vector database.
 * @summary Create Recommendation Context
 */
export const createRecommendationContextHeaderXLangDefault = `es`;

export const CreateRecommendationContextHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(createRecommendationContextHeaderXLangDefault).describe('Language code'),
});

export const createRecommendationContextBodyQuestionMin = 2;
export const createRecommendationContextBodyQuestionMax = 2500;

export const createRecommendationContextBodyAnswerMin = 2;
export const createRecommendationContextBodyAnswerMax = 2500;

export const CreateRecommendationContextBody = zod
  .object({
    question: zod
      .string()
      .min(createRecommendationContextBodyQuestionMin)
      .max(createRecommendationContextBodyQuestionMax)
      .describe('Question text stored as context metadata.'),
    answer: zod
      .string()
      .min(createRecommendationContextBodyAnswerMin)
      .max(createRecommendationContextBodyAnswerMax)
      .describe('Answer text stored as context metadata.'),
    tags: zod.array(zod.array()).nullish().describe('Optional tags associated with the recommendation context for categorization and searchability.'),
  })
  .describe('Payload used to create a recommendation context.');

export const CreateRecommendationContextResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation context identifier (UUID format)'),
    question: zod.string().describe('Context question used to build the vector search record'),
    answer: zod.string().describe('Context answer associated with the question'),
    tags: zod.array(zod.string()).nullable().describe('Optional tags for categorizing or filtering recommendation contexts'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of context creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last context update'),
  })
  .describe('Represents a recommendation context mirrored from the vector database for fast relational access');

/**
 * Retrieves a single recommendation context by identifier.
 * @summary Get Recommendation Context
 */
export const GetRecommendationContextParams = zod.object({
  contextId: zod.string().describe('Recommendation context identifier (UUID).'),
});

export const getRecommendationContextHeaderXLangDefault = `es`;

export const GetRecommendationContextHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getRecommendationContextHeaderXLangDefault).describe('Language code'),
});

export const GetRecommendationContextResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation context identifier (UUID format)'),
    question: zod.string().describe('Context question used to build the vector search record'),
    answer: zod.string().describe('Context answer associated with the question'),
    tags: zod.array(zod.string()).nullable().describe('Optional tags for categorizing or filtering recommendation contexts'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of context creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last context update'),
  })
  .describe('Represents a recommendation context mirrored from the vector database for fast relational access');

/**
 * Updates an existing recommendation context and re-synchronizes it in the vector database.
 * @summary Update Recommendation Context
 */
export const PatchRecommendationContextParams = zod.object({
  contextId: zod.string().describe('Recommendation context identifier (UUID).'),
});

export const patchRecommendationContextHeaderXLangDefault = `es`;

export const PatchRecommendationContextHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(patchRecommendationContextHeaderXLangDefault).describe('Language code'),
});

export const patchRecommendationContextBodyQuestionMin = 2;
export const patchRecommendationContextBodyQuestionMax = 2500;

export const patchRecommendationContextBodyAnswerMin = 2;
export const patchRecommendationContextBodyAnswerMax = 2500;

export const PatchRecommendationContextBody = zod
  .object({
    question: zod
      .string()
      .min(patchRecommendationContextBodyQuestionMin)
      .max(patchRecommendationContextBodyQuestionMax)
      .optional()
      .describe('Updated question text stored as context metadata.'),
    answer: zod
      .string()
      .min(patchRecommendationContextBodyAnswerMin)
      .max(patchRecommendationContextBodyAnswerMax)
      .optional()
      .describe('Updated answer text stored as context metadata.'),
    tags: zod
      .array(zod.array())
      .nullish()
      .describe('Updated optional tags associated with the recommendation context for categorization and searchability.'),
  })
  .describe('Editable fields of an existing recommendation context.');

export const PatchRecommendationContextResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation context identifier (UUID format)'),
    question: zod.string().describe('Context question used to build the vector search record'),
    answer: zod.string().describe('Context answer associated with the question'),
    tags: zod.array(zod.string()).nullable().describe('Optional tags for categorizing or filtering recommendation contexts'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of context creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last context update'),
  })
  .describe('Represents a recommendation context mirrored from the vector database for fast relational access');

/**
 * Soft deletes a recommendation context and removes its mirrored record from the vector database.
 * @summary Delete Recommendation Context
 */
export const DeleteRecommendationContextParams = zod.object({
  contextId: zod.string().describe('Recommendation context identifier (UUID).'),
});

export const deleteRecommendationContextHeaderXLangDefault = `es`;

export const DeleteRecommendationContextHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(deleteRecommendationContextHeaderXLangDefault).describe('Language code'),
});

export const DeleteRecommendationContextResponse = zod
  .object({
    id: zod.string().describe('Unique recommendation context identifier (UUID format)'),
    question: zod.string().describe('Context question used to build the vector search record'),
    answer: zod.string().describe('Context answer associated with the question'),
    tags: zod.array(zod.string()).nullable().describe('Optional tags for categorizing or filtering recommendation contexts'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of context creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last context update'),
  })
  .describe('Represents a recommendation context mirrored from the vector database for fast relational access');

/**
 * Redirects to Apple authentication page to initiate OAuth2 sign-in flow
 * @summary Initiate Apple OAuth2 Login
 */
export const appleLoginHeaderXLangDefault = `es`;

export const AppleLoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(appleLoginHeaderXLangDefault).describe('Language code'),
});

export const AppleLoginResponse = zod.void();

/**
 * Handles the OAuth2 callback from Apple after successful user authentication
 * @summary Apple OAuth2 Callback Handler
 */
export const appleLoginCallbackHeaderXLangDefault = `es`;

export const AppleLoginCallbackHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(appleLoginCallbackHeaderXLangDefault).describe('Language code'),
});

export const AppleLoginCallbackResponse = zod.void();

/**
 * Authenticates or registers users via Apple OAuth2 for mobile apps and external clients
 * @summary Apple OAuth2 Mobile/External Login
 */
export const appleExternalLoginHeaderXLangDefault = `es`;

export const AppleExternalLoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(appleExternalLoginHeaderXLangDefault).describe('Language code'),
});

export const AppleExternalLoginBody = zod
  .object({
    token: zod.string().describe('Apple OAuth2 identity token received from the Apple SDK'),
  })
  .describe('Contains Apple identity token for authentication verification');

export const AppleExternalLoginResponse = zod
  .object({
    access_token: zod.string().describe('JWT access token for accessing protected API resources'),
    refresh_token: zod.string().describe('JWT refresh token for obtaining new access tokens when the current one expires'),
    access_expires_in: zod.number().describe('Access token expiration duration in seconds'),
    refresh_expires_in: zod.number().describe('Refresh token expiration duration in seconds'),
  })
  .describe('JWT tokens issued after successful Apple authentication');

/**
 * Redirects to Google authentication page to initiate OAuth2 sign-in flow
 * @summary Initiate Google OAuth2 Login
 */
export const googleLoginHeaderXLangDefault = `es`;

export const GoogleLoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(googleLoginHeaderXLangDefault).describe('Language code'),
});

export const GoogleLoginResponse = zod.void();

/**
 * Handles the OAuth2 callback from Google after successful user authentication
 * @summary Google OAuth2 Callback Handler
 */
export const googleLoginCallbackHeaderXLangDefault = `es`;

export const GoogleLoginCallbackHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(googleLoginCallbackHeaderXLangDefault).describe('Language code'),
});

export const GoogleLoginCallbackResponse = zod.void();

/**
 * Authenticates or registers users via Google OAuth2 for mobile apps and external clients
 * @summary Google OAuth2 Mobile/External Login
 */
export const googleExternalLoginHeaderXLangDefault = `es`;

export const GoogleExternalLoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(googleExternalLoginHeaderXLangDefault).describe('Language code'),
});

export const GoogleExternalLoginBody = zod
  .object({
    token: zod.string().describe('Google OAuth2 ID token received from the Google SDK'),
  })
  .describe('Contains Google ID token for authentication verification');

export const GoogleExternalLoginResponse = zod
  .object({
    access_token: zod.string().describe('JWT access token for accessing protected API resources'),
    refresh_token: zod.string().describe('JWT refresh token for obtaining new access tokens when the current one expires'),
    access_expires_in: zod.number().describe('Access token expiration duration in seconds'),
    refresh_expires_in: zod.number().describe('Refresh token expiration duration in seconds'),
  })
  .describe('JWT tokens issued after successful Google authentication');

/**
 * Creates a temporary TOTP (Time-based One-Time Password) secret for Multi-Factor Authentication setup
 * @summary Generate MFA Secret
 */
export const generateMfaHeaderXLangDefault = `es`;

export const GenerateMfaHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(generateMfaHeaderXLangDefault).describe('Language code'),
});

export const GenerateMfaResponse = zod
  .object({
    base32: zod.string().describe('Base32-encoded TOTP secret key for manual entry in authenticator apps'),
    otpauth_url: zod.string().describe('OTPAuth URL (URI format) for direct import into authenticator applications'),
    qr: zod.string().describe('QR code image (Base64-encoded) for authenticator app scanning'),
  })
  .describe('TOTP secret key and QR code for authenticator app setup');

/**
 * Activates MFA after verifying the TOTP token and credentials
 * @summary Enable Multi-Factor Authentication
 */
export const enableMfaHeaderXLangDefault = `es`;

export const EnableMfaHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(enableMfaHeaderXLangDefault).describe('Language code'),
});

export const enableMfaBodyPasswordMin = 6;
export const enableMfaBodyPasswordMax = 36;

export const EnableMfaBody = zod
  .object({
    password: zod.string().min(enableMfaBodyPasswordMin).max(enableMfaBodyPasswordMax).describe('User account password for identity verification'),
    token: zod.string().describe('TOTP code (6-digit) from authenticator application'),
    base32: zod.string().describe('Temporary TOTP secret (Base32-encoded) from the generation step'),
  })
  .describe('Credentials and TOTP token required to verify and enable MFA');

export const EnableMfaResponse = zod
  .object({
    backup_codes: zod.array(zod.string()).describe('Array of one-time backup codes for account recovery if authenticator device is lost'),
  })
  .describe('Backup codes and confirmation after successful MFA activation');

/**
 * Deactivates MFA for the user account after credential verification
 * @summary Disable Multi-Factor Authentication
 */
export const disableMfaHeaderXLangDefault = `es`;

export const DisableMfaHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(disableMfaHeaderXLangDefault).describe('Language code'),
});

export const disableMfaBodyPasswordMin = 6;
export const disableMfaBodyPasswordMax = 36;

export const DisableMfaBody = zod
  .object({
    password: zod.string().min(disableMfaBodyPasswordMin).max(disableMfaBodyPasswordMax).describe('User account password for identity verification'),
    token: zod.string().describe('TOTP code (6-digit) from authenticator application'),
  })
  .describe('Credentials required to disable MFA');

export const DisableMfaResponse = zod.void();

/**
 * Completes the login process by verifying the MFA token to establish authenticated session
 * @summary Verify MFA During Login
 */
export const validateMfaLoginHeaderXLangDefault = `es`;

export const ValidateMfaLoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(validateMfaLoginHeaderXLangDefault).describe('Language code'),
});

export const ValidateMfaLoginBody = zod
  .object({
    otp_session_id: zod.string().describe('Temporary session ID (UUID format) received from initial login attempt'),
    token: zod.string().describe('TOTP code (6-digit) from authenticator application'),
  })
  .describe('Session ID and TOTP token for login completion');

export const ValidateMfaLoginResponse = zod
  .object({
    access_token: zod.string().describe('JWT access token for accessing protected API resources'),
    refresh_token: zod.string().describe('JWT refresh token for obtaining new access tokens when expired'),
    access_expires_in: zod.number().describe('Access token expiration duration in seconds'),
    refresh_expires_in: zod.number().describe('Refresh token expiration duration in seconds'),
    type: zod.enum(['totp', 'backup']).describe('MFA verification method used (TOTP, backup code, etc.)'),
  })
  .describe('JWT tokens issued after successful MFA verification');

/**
 * Generates new backup codes and invalidates all previous backup codes
 * @summary Regenerate MFA Backup Codes
 */
export const regenerateMfaBackupCodesHeaderXLangDefault = `es`;

export const RegenerateMfaBackupCodesHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(regenerateMfaBackupCodesHeaderXLangDefault).describe('Language code'),
});

export const regenerateMfaBackupCodesBodyPasswordMin = 6;
export const regenerateMfaBackupCodesBodyPasswordMax = 36;

export const RegenerateMfaBackupCodesBody = zod
  .object({
    password: zod
      .string()
      .min(regenerateMfaBackupCodesBodyPasswordMin)
      .max(regenerateMfaBackupCodesBodyPasswordMax)
      .describe('User account password for identity confirmation'),
    token: zod.string().describe('TOTP code (6-digit) from authenticator application'),
  })
  .describe('Credentials required to regenerate backup codes');

export const RegenerateMfaBackupCodesResponse = zod
  .object({
    backup_codes: zod.array(zod.string()).describe('Array of new one-time backup codes'),
  })
  .describe('Newly generated backup codes for account recovery');

/**
 * Sends a password reset link to the user email address to initiate password recovery
 * @summary Request Password Reset Link
 */
export const forgotPasswordHeaderXLangDefault = `es`;

export const ForgotPasswordHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(forgotPasswordHeaderXLangDefault).describe('Language code'),
});

export const ForgotPasswordBody = zod
  .object({
    email: zod.string().describe('Email address associated with the user account'),
  })
  .describe('Email address to receive the password reset link');

export const ForgotPasswordResponse = zod.void();

/**
 * Sets a new password using a valid reset token sent via email
 * @summary Confirm Password Reset
 */
export const resetPasswordHeaderXLangDefault = `es`;

export const ResetPasswordHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(resetPasswordHeaderXLangDefault).describe('Language code'),
});

export const resetPasswordBodyNewPasswordMin = 6;
export const resetPasswordBodyNewPasswordMax = 36;

export const ResetPasswordBody = zod
  .object({
    token: zod.string().describe('Password reset token from the email link'),
    new_password: zod
      .string()
      .min(resetPasswordBodyNewPasswordMin)
      .max(resetPasswordBodyNewPasswordMax)
      .describe('The new password to set for the account'),
  })
  .describe('Reset token and new password for account recovery');

export const ResetPasswordResponse = zod.void();

/**
 * Updates the password for an authenticated user with MFA support
 * @summary Change Account Password
 */
export const updatePasswordHeaderXLangDefault = `es`;

export const UpdatePasswordHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(updatePasswordHeaderXLangDefault).describe('Language code'),
});

export const updatePasswordBodyCurrentPasswordMin = 6;
export const updatePasswordBodyCurrentPasswordMax = 36;

export const updatePasswordBodyNewPasswordMin = 6;
export const updatePasswordBodyNewPasswordMax = 36;

export const UpdatePasswordBody = zod
  .object({
    current_password: zod
      .string()
      .min(updatePasswordBodyCurrentPasswordMin)
      .max(updatePasswordBodyCurrentPasswordMax)
      .describe('Current account password for verification'),
    new_password: zod
      .string()
      .min(updatePasswordBodyNewPasswordMin)
      .max(updatePasswordBodyNewPasswordMax)
      .describe('New password to set for the account'),
    token: zod.string().optional().describe('TOTP code (6-digit, required if MFA is enabled on account)'),
  })
  .describe('Current password and new password for authenticated user');

export const UpdatePasswordResponse = zod.void();

/**
 * Creates a password for OAuth users who registered without a password
 * @summary Set Initial Password
 */
export const setPasswordHeaderXLangDefault = `es`;

export const SetPasswordHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(setPasswordHeaderXLangDefault).describe('Language code'),
});

export const setPasswordBodyNewPasswordMin = 6;
export const setPasswordBodyNewPasswordMax = 36;

export const SetPasswordBody = zod
  .object({
    new_password: zod.string().min(setPasswordBodyNewPasswordMin).max(setPasswordBodyNewPasswordMax).describe('The password to set for the account'),
  })
  .describe('Initial password for OAuth users');

export const SetPasswordResponse = zod.void();

/**
 * Registers a new user account with credentials and required client profile fields
 * @summary Create New User Account
 */
export const registerUserHeaderXLangDefault = `es`;

export const RegisterUserHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(registerUserHeaderXLangDefault).describe('Language code'),
});

export const registerUserBodyPasswordMin = 6;
export const registerUserBodyPasswordMax = 36;

export const registerUserBodyUsernameMin = 3;
export const registerUserBodyUsernameMax = 36;

export const registerUserBodyUsernameRegExp = new RegExp('^[a-z0-9]+$');
export const registerUserBodyDisplayNameMin = 3;
export const registerUserBodyDisplayNameMax = 50;

export const registerUserBodyPhoneRegExp = new RegExp('^\\+[1-9]\\d{7,14}$');
export const registerUserBodyAgeMin = 14;
export const registerUserBodyAgeMax = 120;

export const RegisterUserBody = zod
  .object({
    email: zod.string().describe('Email address for account login and verification'),
    password: zod
      .string()
      .min(registerUserBodyPasswordMin)
      .max(registerUserBodyPasswordMax)
      .describe('Account password (must meet security requirements)'),
    username: zod
      .string()
      .min(registerUserBodyUsernameMin)
      .max(registerUserBodyUsernameMax)
      .regex(registerUserBodyUsernameRegExp)
      .optional()
      .describe('Unique username for public identification'),
    display_name: zod
      .string()
      .min(registerUserBodyDisplayNameMin)
      .max(registerUserBodyDisplayNameMax)
      .optional()
      .describe('Public display name shown to other users'),
    phone: zod.string().regex(registerUserBodyPhoneRegExp).describe('Client phone number in international format (E.164)'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).describe('Client gender for profile demographics'),
    age: zod.number().min(registerUserBodyAgeMin).max(registerUserBodyAgeMax).describe('Client age in years'),
  })
  .describe('User information required for account creation');

export const RegisterUserResponse = zod.void();

/**
 * Confirms user email ownership by verifying the token sent via email
 * @summary Verify Email Address
 */
export const verifyEmailHeaderXLangDefault = `es`;

export const VerifyEmailHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(verifyEmailHeaderXLangDefault).describe('Language code'),
});

export const VerifyEmailBody = zod
  .object({
    token: zod.string().describe('Email verification token sent to user email'),
  })
  .describe('Email verification token from the email link');

export const VerifyEmailResponse = zod.void();

/**
 * Retrieves a paginated list of all users, each including their complete login session log history. Restricted to technical support
 * @summary List All Users With Session Logs
 */
export const getAllUsersSessionLogsQueryOrderDirectionDefault = `ASC`;
export const getAllUsersSessionLogsQueryOrderByDefault = `id`;
export const getAllUsersSessionLogsQueryPageDefault = 1;

export const getAllUsersSessionLogsQueryLimitDefault = 10;
export const getAllUsersSessionLogsQueryLimitMax = 100;

export const GetAllUsersSessionLogsQueryParams = zod.object({
  orderDirection: zod.enum(['ASC', 'DESC']).default(getAllUsersSessionLogsQueryOrderDirectionDefault).describe('Sorting direction (ASC or DESC)'),
  orderBy: zod
    .enum([
      'id',
      'username',
      'displayName',
      'isEmailVerified',
      'lastLoginAt',
      'roles',
      'roles.id',
      'roles.name',
      'picture',
      'picture.id',
      'picture.url',
      'picture.jobId',
      'picture.processing',
      'picture.origin',
      'picture.createdAt',
      'picture.updatedAt',
      'createdAt',
      'updatedAt',
      'email',
      'sessionLogs',
      'sessionLogs.id',
      'sessionLogs.sessionId',
      'sessionLogs.status',
      'sessionLogs.failureReason',
      'sessionLogs.ipAddress',
      'sessionLogs.country',
      'sessionLogs.city',
      'sessionLogs.browser',
      'sessionLogs.os',
      'sessionLogs.deviceType',
      'sessionLogs.revokedAt',
      'sessionLogs.createdAt',
    ])
    .default(getAllUsersSessionLogsQueryOrderByDefault)
    .describe('User field to sort by (e.g., username, createdAt)'),
  page: zod.number().min(1).default(getAllUsersSessionLogsQueryPageDefault).describe('Page number for pagination (starts at 1)'),
  limit: zod
    .number()
    .min(1)
    .max(getAllUsersSessionLogsQueryLimitMax)
    .default(getAllUsersSessionLogsQueryLimitDefault)
    .describe('Number of users per page'),
  select: zod
    .array(
      zod.enum([
        'id',
        'username',
        'displayName',
        'isEmailVerified',
        'lastLoginAt',
        'roles',
        'roles.id',
        'roles.name',
        'picture',
        'picture.id',
        'picture.url',
        'picture.jobId',
        'picture.processing',
        'picture.origin',
        'picture.createdAt',
        'picture.updatedAt',
        'createdAt',
        'updatedAt',
        'email',
        'sessionLogs',
        'sessionLogs.id',
        'sessionLogs.sessionId',
        'sessionLogs.status',
        'sessionLogs.failureReason',
        'sessionLogs.ipAddress',
        'sessionLogs.country',
        'sessionLogs.city',
        'sessionLogs.browser',
        'sessionLogs.os',
        'sessionLogs.deviceType',
        'sessionLogs.revokedAt',
        'sessionLogs.createdAt',
      ]),
    )
    .default([
      `id`,
      `username`,
      `displayName`,
      `isEmailVerified`,
      `lastLoginAt`,
      `roles`,
      `roles.id`,
      `roles.name`,
      `picture`,
      `picture.id`,
      `picture.url`,
      `picture.jobId`,
      `picture.processing`,
      `picture.origin`,
      `picture.createdAt`,
      `picture.updatedAt`,
      `createdAt`,
      `updatedAt`,
      `email`,
      `sessionLogs`,
      `sessionLogs.id`,
      `sessionLogs.sessionId`,
      `sessionLogs.status`,
      `sessionLogs.failureReason`,
      `sessionLogs.ipAddress`,
      `sessionLogs.country`,
      `sessionLogs.city`,
      `sessionLogs.browser`,
      `sessionLogs.os`,
      `sessionLogs.deviceType`,
      `sessionLogs.revokedAt`,
      `sessionLogs.createdAt`,
    ])
    .describe(
      'User columns to include in response. Use repeated query keys (e.g., ?select=id&select=username). Nested relations (roles, picture, sessionLogs) can be selected in full (e.g., ?select=picture) or by field (e.g., ?select=picture.url); a relation is only joined when it, or one of its fields, is selected. Defaults to all public columns.',
    ),
});

export const getAllUsersSessionLogsHeaderXLangDefault = `es`;

export const GetAllUsersSessionLogsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getAllUsersSessionLogsHeaderXLangDefault).describe('Language code'),
});

export const getAllUsersSessionLogsResponseUsersItemUsernameMin = 3;
export const getAllUsersSessionLogsResponseUsersItemUsernameMax = 36;

export const getAllUsersSessionLogsResponseUsersItemDisplayNameMin = 3;
export const getAllUsersSessionLogsResponseUsersItemDisplayNameMax = 50;

export const getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneUsernameMin = 3;
export const getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneUsernameMax = 36;

export const getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneDisplayNameMin = 3;
export const getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneDisplayNameMax = 50;

export const GetAllUsersSessionLogsResponse = zod
  .object({
    total: zod.number().describe('Total number of users'),
    page: zod.number().describe('Current page number'),
    limit: zod.number().describe('Number of users per page'),
    total_pages: zod.number().describe('Total number of pages available'),
    users: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique user identifier (UUID format)'),
            username: zod
              .string()
              .min(getAllUsersSessionLogsResponseUsersItemUsernameMin)
              .max(getAllUsersSessionLogsResponseUsersItemUsernameMax)
              .describe('Unique username for login and identification'),
            display_name: zod
              .string()
              .min(getAllUsersSessionLogsResponseUsersItemDisplayNameMin)
              .max(getAllUsersSessionLogsResponseUsersItemDisplayNameMax)
              .nullable()
              .describe('Public name displayed for other users'),
            is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
            roles: zod
              .array(
                zod
                  .object({
                    id: zod.string().describe('Unique role identifier (UUID format)'),
                    name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
                  })
                  .describe('Represents a role or permission level assigned to a user'),
              )
              .describe('List of roles and permission levels assigned to the user'),
            picture: zod
              .union([
                zod
                  .object({
                    id: zod.string().describe('Unique picture identifier (UUID format)'),
                    url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
                    job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
                    processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
                    origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
                    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
                    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
                  })
                  .describe('Represents a user profile picture or avatar image'),
                zod.null(),
              ])
              .describe('User profile picture or avatar information'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
            session_logs: zod
              .array(
                zod
                  .object({
                    id: zod.string().describe('Unique session log identifier (UUID format)'),
                    session_id: zod
                      .string()
                      .nullable()
                      .describe('Stable session identifier shared with the access\/refresh token pair created on a successful login'),
                    status: zod.enum(['success', 'failed']).describe('Outcome of the login attempt (SUCCESS or FAILED)'),
                    failure_reason: zod
                      .enum(['invalid_password', 'invalid_mfa_token'])
                      .nullable()
                      .describe('Reason the login attempt failed (null when the attempt succeeded)'),
                    ip_address: zod.string().nullable().describe('IP address the login attempt originated from'),
                    browser: zod.string().nullable().describe('Browser parsed from the User-Agent header'),
                    os: zod.string().nullable().describe('Operating system parsed from the User-Agent header'),
                    device_type: zod.string().nullable().describe('Device type parsed from the User-Agent header (mobile, tablet, desktop, etc.)'),
                    country: zod.string().nullable().describe('Country resolved from the IP address'),
                    city: zod.string().nullable().describe('City resolved from the IP address'),
                    user: zod
                      .object({
                        id: zod.string().describe('Unique user identifier (UUID format)'),
                        username: zod
                          .string()
                          .min(getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneUsernameMin)
                          .max(getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneUsernameMax)
                          .describe('Unique username for login and identification'),
                        display_name: zod
                          .string()
                          .min(getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneDisplayNameMin)
                          .max(getAllUsersSessionLogsResponseUsersItemSessionLogsItemUserOneDisplayNameMax)
                          .nullable()
                          .describe('Public name displayed for other users'),
                        is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
                        roles: zod
                          .array(
                            zod
                              .object({
                                id: zod.string().describe('Unique role identifier (UUID format)'),
                                name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
                              })
                              .describe('Represents a role or permission level assigned to a user'),
                          )
                          .describe('List of roles and permission levels assigned to the user'),
                        picture: zod
                          .union([
                            zod
                              .object({
                                id: zod.string().describe('Unique picture identifier (UUID format)'),
                                url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
                                job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
                                processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
                                origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
                                created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
                                updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
                              })
                              .describe('Represents a user profile picture or avatar image'),
                            zod.null(),
                          ])
                          .describe('User profile picture or avatar information'),
                        created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
                      })
                      .describe('Publicly visible user profile information shown to other users')
                      .describe('User account this login attempt is associated with'),
                    revoked_at: zod.iso
                      .datetime({ offset: true })
                      .nullable()
                      .describe('ISO 8601 timestamp when this session was closed (null while active)'),
                    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of the login attempt'),
                  })
                  .describe('Represents a login attempt (successful or failed) recorded for auditing and session management'),
              )
              .describe('List of session logs associated with the user account'),
          })
          .describe('User profile information including associated session logs for auditing and management'),
      )
      .describe('Array of users, each including all of their login session logs'),
  })
  .describe('Paginated list of users, each including all of their login session logs');

/**
 * Retrieves the login session log tied to the access token used for this request
 * @summary Get Current Session
 */
export const getCurrentSessionLogHeaderXLangDefault = `es`;

export const GetCurrentSessionLogHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getCurrentSessionLogHeaderXLangDefault).describe('Language code'),
});

export const getCurrentSessionLogResponseUserOneUsernameMin = 3;
export const getCurrentSessionLogResponseUserOneUsernameMax = 36;

export const getCurrentSessionLogResponseUserOneDisplayNameMin = 3;
export const getCurrentSessionLogResponseUserOneDisplayNameMax = 50;

export const GetCurrentSessionLogResponse = zod
  .object({
    id: zod.string().describe('Unique session log identifier (UUID format)'),
    session_id: zod
      .string()
      .nullable()
      .describe('Stable session identifier shared with the access\/refresh token pair created on a successful login'),
    status: zod.enum(['success', 'failed']).describe('Outcome of the login attempt (SUCCESS or FAILED)'),
    failure_reason: zod
      .enum(['invalid_password', 'invalid_mfa_token'])
      .nullable()
      .describe('Reason the login attempt failed (null when the attempt succeeded)'),
    ip_address: zod.string().nullable().describe('IP address the login attempt originated from'),
    browser: zod.string().nullable().describe('Browser parsed from the User-Agent header'),
    os: zod.string().nullable().describe('Operating system parsed from the User-Agent header'),
    device_type: zod.string().nullable().describe('Device type parsed from the User-Agent header (mobile, tablet, desktop, etc.)'),
    country: zod.string().nullable().describe('Country resolved from the IP address'),
    city: zod.string().nullable().describe('City resolved from the IP address'),
    user: zod
      .object({
        id: zod.string().describe('Unique user identifier (UUID format)'),
        username: zod
          .string()
          .min(getCurrentSessionLogResponseUserOneUsernameMin)
          .max(getCurrentSessionLogResponseUserOneUsernameMax)
          .describe('Unique username for login and identification'),
        display_name: zod
          .string()
          .min(getCurrentSessionLogResponseUserOneDisplayNameMin)
          .max(getCurrentSessionLogResponseUserOneDisplayNameMax)
          .nullable()
          .describe('Public name displayed for other users'),
        is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
        roles: zod
          .array(
            zod
              .object({
                id: zod.string().describe('Unique role identifier (UUID format)'),
                name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
              })
              .describe('Represents a role or permission level assigned to a user'),
          )
          .describe('List of roles and permission levels assigned to the user'),
        picture: zod
          .union([
            zod
              .object({
                id: zod.string().describe('Unique picture identifier (UUID format)'),
                url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
                job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
                processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
                origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
                created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
                updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
              })
              .describe('Represents a user profile picture or avatar image'),
            zod.null(),
          ])
          .describe('User profile picture or avatar information'),
        created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
      })
      .describe('Publicly visible user profile information shown to other users')
      .describe('User account this login attempt is associated with'),
    revoked_at: zod.iso.datetime({ offset: true }).nullable().describe('ISO 8601 timestamp when this session was closed (null while active)'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of the login attempt'),
  })
  .describe('Represents a login attempt (successful or failed) recorded for auditing and session management');

/**
 * Retrieves a paginated list of login attempts (successful and failed) for a specific user or the current user
 * @summary List Login Session Logs
 */
export const GetAllSessionLogsParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format) or \"me\" for the current user'),
});

export const getAllSessionLogsQueryOrderDirectionDefault = `DESC`;
export const getAllSessionLogsQueryOrderByDefault = `createdAt`;
export const getAllSessionLogsQueryPageDefault = 1;

export const getAllSessionLogsQueryLimitDefault = 10;

export const GetAllSessionLogsQueryParams = zod.object({
  orderDirection: zod.enum(['ASC', 'DESC']).default(getAllSessionLogsQueryOrderDirectionDefault).describe('Sorting direction (ASC or DESC)'),
  orderBy: zod
    .enum([
      'id',
      'sessionId',
      'status',
      'failureReason',
      'ipAddress',
      'country',
      'city',
      'browser',
      'os',
      'deviceType',
      'revokedAt',
      'createdAt',
      'user',
      'user.id',
      'user.username',
      'user.displayName',
      'user.isEmailVerified',
      'user.createdAt',
    ])
    .default(getAllSessionLogsQueryOrderByDefault)
    .describe('Field to sort by (e.g., createdAt, status)'),
  page: zod.number().min(1).default(getAllSessionLogsQueryPageDefault).describe('Page number for pagination (starts at 1)'),
  limit: zod.number().min(1).default(getAllSessionLogsQueryLimitDefault).describe('Number of results per page'),
  select: zod
    .array(
      zod.enum([
        'id',
        'sessionId',
        'status',
        'failureReason',
        'ipAddress',
        'country',
        'city',
        'browser',
        'os',
        'deviceType',
        'revokedAt',
        'createdAt',
        'user',
        'user.id',
        'user.username',
        'user.displayName',
        'user.isEmailVerified',
        'user.createdAt',
      ]),
    )
    .default([
      `id`,
      `sessionId`,
      `status`,
      `failureReason`,
      `ipAddress`,
      `country`,
      `city`,
      `browser`,
      `os`,
      `deviceType`,
      `revokedAt`,
      `createdAt`,
      `user`,
      `user.id`,
      `user.username`,
      `user.displayName`,
      `user.isEmailVerified`,
      `user.createdAt`,
    ])
    .describe(
      'Columns to include in response. Use repeated query keys (e.g., ?select=id&select=status). The nested user can be selected in full (e.g., ?select=user) or by field (e.g., ?select=user.username); it is only joined when selected. Defaults to all columns.',
    ),
});

export const getAllSessionLogsHeaderXLangDefault = `es`;

export const GetAllSessionLogsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getAllSessionLogsHeaderXLangDefault).describe('Language code'),
});

export const getAllSessionLogsResponseSessionLogsItemUserOneUsernameMin = 3;
export const getAllSessionLogsResponseSessionLogsItemUserOneUsernameMax = 36;

export const getAllSessionLogsResponseSessionLogsItemUserOneDisplayNameMin = 3;
export const getAllSessionLogsResponseSessionLogsItemUserOneDisplayNameMax = 50;

export const GetAllSessionLogsResponse = zod
  .object({
    total: zod.number().describe('Total number of session logs for this user'),
    page: zod.number().describe('Current page number'),
    limit: zod.number().describe('Number of session logs per page'),
    total_pages: zod.number().describe('Total number of pages available'),
    session_logs: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique session log identifier (UUID format)'),
            session_id: zod
              .string()
              .nullable()
              .describe('Stable session identifier shared with the access\/refresh token pair created on a successful login'),
            status: zod.enum(['success', 'failed']).describe('Outcome of the login attempt (SUCCESS or FAILED)'),
            failure_reason: zod
              .enum(['invalid_password', 'invalid_mfa_token'])
              .nullable()
              .describe('Reason the login attempt failed (null when the attempt succeeded)'),
            ip_address: zod.string().nullable().describe('IP address the login attempt originated from'),
            browser: zod.string().nullable().describe('Browser parsed from the User-Agent header'),
            os: zod.string().nullable().describe('Operating system parsed from the User-Agent header'),
            device_type: zod.string().nullable().describe('Device type parsed from the User-Agent header (mobile, tablet, desktop, etc.)'),
            country: zod.string().nullable().describe('Country resolved from the IP address'),
            city: zod.string().nullable().describe('City resolved from the IP address'),
            user: zod
              .object({
                id: zod.string().describe('Unique user identifier (UUID format)'),
                username: zod
                  .string()
                  .min(getAllSessionLogsResponseSessionLogsItemUserOneUsernameMin)
                  .max(getAllSessionLogsResponseSessionLogsItemUserOneUsernameMax)
                  .describe('Unique username for login and identification'),
                display_name: zod
                  .string()
                  .min(getAllSessionLogsResponseSessionLogsItemUserOneDisplayNameMin)
                  .max(getAllSessionLogsResponseSessionLogsItemUserOneDisplayNameMax)
                  .nullable()
                  .describe('Public name displayed for other users'),
                is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
                roles: zod
                  .array(
                    zod
                      .object({
                        id: zod.string().describe('Unique role identifier (UUID format)'),
                        name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
                      })
                      .describe('Represents a role or permission level assigned to a user'),
                  )
                  .describe('List of roles and permission levels assigned to the user'),
                picture: zod
                  .union([
                    zod
                      .object({
                        id: zod.string().describe('Unique picture identifier (UUID format)'),
                        url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
                        job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
                        processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
                        origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
                        created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
                        updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
                      })
                      .describe('Represents a user profile picture or avatar image'),
                    zod.null(),
                  ])
                  .describe('User profile picture or avatar information'),
                created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
              })
              .describe('Publicly visible user profile information shown to other users')
              .describe('User account this login attempt is associated with'),
            revoked_at: zod.iso.datetime({ offset: true }).nullable().describe('ISO 8601 timestamp when this session was closed (null while active)'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of the login attempt'),
          })
          .describe('Represents a login attempt (successful or failed) recorded for auditing and session management'),
      )
      .describe('Array of login session log objects'),
  })
  .describe('Paginated list of login session logs with metadata');

/**
 * Closes every other active login session for a user. For "me" this excludes the session tied to the current access token; for another user (admin only) every active session is closed, since the request carries no session of theirs to preserve
 * @summary Close Other Sessions
 */
export const CloseOtherSessionsParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format) or \"me\" for the current user'),
});

export const closeOtherSessionsHeaderXLangDefault = `es`;

export const CloseOtherSessionsHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(closeOtherSessionsHeaderXLangDefault).describe('Language code'),
});

export const CloseOtherSessionsResponse = zod
  .object({
    closed: zod.number().describe('Number of sessions that were closed'),
  })
  .describe('Result of closing other active sessions');

/**
 * Closes one specific active login session for a user and blacklists its tokens
 * @summary Close Session
 */
export const CloseSessionParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format) or \"me\" for the current user'),
  sessionId: zod.string().describe('Session ID to close, as returned in the \"sessionId\" field of a session log'),
});

export const closeSessionHeaderXLangDefault = `es`;

export const CloseSessionHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(closeSessionHeaderXLangDefault).describe('Language code'),
});

export const CloseSessionResponse = zod.void();

/**
 * Authenticates a user with credentials and returns JWT tokens for session management
 * @summary User Login
 */
export const loginHeaderXLangDefault = `es`;

export const LoginHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(loginHeaderXLangDefault).describe('Language code'),
});

export const loginBodyPasswordMin = 6;
export const loginBodyPasswordMax = 36;

export const LoginBody = zod
  .object({
    identifier: zod.string().describe('User email address or username'),
    password: zod.string().min(loginBodyPasswordMin).max(loginBodyPasswordMax).describe('User account password'),
  })
  .describe('User credentials for authentication');

export const LoginResponse = zod
  .object({
    access_token: zod.string().describe('JWT access token for accessing protected API resources'),
    refresh_token: zod.string().describe('JWT refresh token for obtaining new access tokens when expired'),
    access_expires_in: zod.number().describe('Access token expiration duration in seconds'),
    refresh_expires_in: zod.number().describe('Refresh token expiration duration in seconds'),
  })
  .describe('JWT tokens and session information after successful authentication');

/**
 * Invalidates the refresh token and ends the authenticated session
 * @summary User Logout
 */
export const logoutHeaderXLangDefault = `es`;

export const LogoutHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(logoutHeaderXLangDefault).describe('Language code'),
});

export const LogoutBody = zod
  .object({
    token: zod.string().describe('Refresh token to invalidate and end the session'),
  })
  .describe('Token to revoke during logout');

export const LogoutResponse = zod.void();

/**
 * Generates new access and refresh tokens using a valid refresh token
 * @summary Refresh JWT Tokens
 */
export const refreshTokensHeaderXLangDefault = `es`;

export const RefreshTokensHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(refreshTokensHeaderXLangDefault).describe('Language code'),
});

export const RefreshTokensBody = zod
  .object({
    token: zod.string().describe('Valid refresh token from a previous authentication'),
  })
  .describe('Refresh token to exchange for new tokens');

export const RefreshTokensResponse = zod
  .object({
    access_token: zod.string().describe('New JWT access token for API resource access'),
    refresh_token: zod.string().describe('New JWT refresh token for future token refreshes'),
    access_expires_in: zod.number().describe('New access token expiration duration in seconds'),
    refresh_expires_in: zod.number().describe('New refresh token expiration duration in seconds'),
  })
  .describe('New JWT tokens for continued session access');

/**
 * Retrieves client information for a specific client or current authenticated user
 * @summary Get Client Details
 */
export const GetClientParams = zod.object({
  identifier: zod.string().describe('Client ID (UUID format), user ID, or \"me\" for current user client'),
});

export const getClientHeaderXLangDefault = `es`;

export const GetClientHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getClientHeaderXLangDefault).describe('Language code'),
});

export const getClientResponseAgeMin = 18;
export const getClientResponseAgeMax = 120;

export const GetClientResponse = zod
  .object({
    id: zod.string().describe('Unique client identifier (UUID format)'),
    phone: zod.string().nullable().describe('Client phone number in international format'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).nullable().describe('Client gender for profile demographics'),
    age: zod.number().min(getClientResponseAgeMin).max(getClientResponseAgeMax).nullable().describe('Client age in years'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of client profile creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last client profile update'),
  })
  .describe('Represents a client profile associated with a user');

/**
 * Partially updates client information
 * @summary Patch Client Information
 */
export const PatchClientParams = zod.object({
  identifier: zod.string().describe('Client ID (UUID format), user ID, or \"me\" for current user client'),
});

export const patchClientHeaderXLangDefault = `es`;

export const PatchClientHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(patchClientHeaderXLangDefault).describe('Language code'),
});

export const patchClientBodyPhoneRegExp = new RegExp('^\\+[1-9]\\d{7,14}$');
export const patchClientBodyAgeMin = 14;
export const patchClientBodyAgeMax = 120;

export const PatchClientBody = zod
  .object({
    phone: zod.string().regex(patchClientBodyPhoneRegExp).optional().describe('Client phone number in international format (E.164)'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).optional().describe('Client gender for profile demographics'),
    age: zod.number().min(patchClientBodyAgeMin).max(patchClientBodyAgeMax).optional().describe('Client age in years'),
  })
  .describe('Client fields to update');

export const patchClientResponseAgeMin = 18;
export const patchClientResponseAgeMax = 120;

export const PatchClientResponse = zod
  .object({
    id: zod.string().describe('Unique client identifier (UUID format)'),
    phone: zod.string().nullable().describe('Client phone number in international format'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).nullable().describe('Client gender for profile demographics'),
    age: zod.number().min(patchClientResponseAgeMin).max(patchClientResponseAgeMax).nullable().describe('Client age in years'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of client profile creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last client profile update'),
  })
  .describe('Represents a client profile associated with a user');

/**
 * Creates a client profile for authenticated users that still do not have one
 * @summary Create Client Profile
 */
export const createClientHeaderXLangDefault = `es`;

export const CreateClientHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(createClientHeaderXLangDefault).describe('Language code'),
});

export const createClientBodyPhoneRegExp = new RegExp('^\\+[1-9]\\d{7,14}$');
export const createClientBodyAgeMin = 14;
export const createClientBodyAgeMax = 120;

export const CreateClientBody = zod
  .object({
    phone: zod.string().regex(createClientBodyPhoneRegExp).describe('Client phone number in international format (E.164)'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).describe('Client gender for profile demographics'),
    age: zod.number().min(createClientBodyAgeMin).max(createClientBodyAgeMax).describe('Client age in years'),
  })
  .describe('Required client profile fields to complete onboarding');

export const createClientResponseAgeMin = 18;
export const createClientResponseAgeMax = 120;

export const CreateClientResponse = zod
  .object({
    id: zod.string().describe('Unique client identifier (UUID format)'),
    phone: zod.string().nullable().describe('Client phone number in international format'),
    gender: zod.enum(['male', 'female', 'prefer_not_to_say', 'other']).nullable().describe('Client gender for profile demographics'),
    age: zod.number().min(createClientResponseAgeMin).max(createClientResponseAgeMax).nullable().describe('Client age in years'),
    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of client profile creation'),
    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last client profile update'),
  })
  .describe('Represents a client profile associated with a user');

/**
 * Retrieves a paginated list of all user accounts in the system
 * @summary List All Users
 */
export const getAllUsersQueryOrderDirectionDefault = `ASC`;
export const getAllUsersQueryOrderByDefault = `id`;
export const getAllUsersQueryPageDefault = 1;

export const getAllUsersQueryLimitDefault = 10;
export const getAllUsersQueryLimitMax = 100;

export const GetAllUsersQueryParams = zod.object({
  orderDirection: zod.enum(['ASC', 'DESC']).default(getAllUsersQueryOrderDirectionDefault).describe('Sorting direction (ASC or DESC)'),
  orderBy: zod
    .enum([
      'id',
      'username',
      'displayName',
      'isEmailVerified',
      'lastLoginAt',
      'roles',
      'roles.id',
      'roles.name',
      'picture',
      'picture.id',
      'picture.url',
      'picture.jobId',
      'picture.processing',
      'picture.origin',
      'picture.createdAt',
      'picture.updatedAt',
      'createdAt',
      'updatedAt',
      'email',
    ])
    .default(getAllUsersQueryOrderByDefault)
    .describe('Field to sort by (e.g., createdAt, username)'),
  page: zod.number().min(1).default(getAllUsersQueryPageDefault).describe('Page number for pagination (starts at 1)'),
  limit: zod.number().min(1).max(getAllUsersQueryLimitMax).default(getAllUsersQueryLimitDefault).describe('Number of results per page'),
  select: zod
    .array(
      zod.enum([
        'id',
        'username',
        'displayName',
        'isEmailVerified',
        'lastLoginAt',
        'roles',
        'roles.id',
        'roles.name',
        'picture',
        'picture.id',
        'picture.url',
        'picture.jobId',
        'picture.processing',
        'picture.origin',
        'picture.createdAt',
        'picture.updatedAt',
        'createdAt',
        'updatedAt',
        'email',
      ]),
    )
    .default([
      `id`,
      `username`,
      `displayName`,
      `isEmailVerified`,
      `lastLoginAt`,
      `roles`,
      `roles.id`,
      `roles.name`,
      `picture`,
      `picture.id`,
      `picture.url`,
      `picture.jobId`,
      `picture.processing`,
      `picture.origin`,
      `picture.createdAt`,
      `picture.updatedAt`,
      `createdAt`,
      `updatedAt`,
      `email`,
    ])
    .describe(
      'Columns to include in response. Use repeated query keys (e.g., ?select=id&select=username). Nested relations (roles, picture) can be selected in full (e.g., ?select=picture) or by field (e.g., ?select=picture.url); a relation is only joined when it, or one of its fields, is selected. Defaults to all public columns.',
    ),
});

export const getAllUsersHeaderXLangDefault = `es`;

export const GetAllUsersHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getAllUsersHeaderXLangDefault).describe('Language code'),
});

export const getAllUsersResponseUsersItemUsernameMin = 3;
export const getAllUsersResponseUsersItemUsernameMax = 36;

export const getAllUsersResponseUsersItemDisplayNameMin = 3;
export const getAllUsersResponseUsersItemDisplayNameMax = 50;

export const GetAllUsersResponse = zod
  .object({
    total: zod.number().describe('Total number of users in the system'),
    page: zod.number().describe('Current page number'),
    limit: zod.number().describe('Number of users per page'),
    total_pages: zod.number().describe('Total number of pages available'),
    users: zod
      .array(
        zod
          .object({
            id: zod.string().describe('Unique user identifier (UUID format)'),
            username: zod
              .string()
              .min(getAllUsersResponseUsersItemUsernameMin)
              .max(getAllUsersResponseUsersItemUsernameMax)
              .describe('Unique username for login and identification'),
            display_name: zod
              .string()
              .min(getAllUsersResponseUsersItemDisplayNameMin)
              .max(getAllUsersResponseUsersItemDisplayNameMax)
              .nullable()
              .describe('Public name displayed for other users'),
            is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
            roles: zod
              .array(
                zod
                  .object({
                    id: zod.string().describe('Unique role identifier (UUID format)'),
                    name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
                  })
                  .describe('Represents a role or permission level assigned to a user'),
              )
              .describe('List of roles and permission levels assigned to the user'),
            picture: zod
              .union([
                zod
                  .object({
                    id: zod.string().describe('Unique picture identifier (UUID format)'),
                    url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
                    job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
                    processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
                    origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
                    created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
                    updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
                  })
                  .describe('Represents a user profile picture or avatar image'),
                zod.null(),
              ])
              .describe('User profile picture or avatar information'),
            created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
          })
          .describe('Publicly visible user profile information shown to other users'),
      )
      .describe('Array of user objects'),
  })
  .describe('Paginated list of users with metadata');

/**
 * Retrieves detailed information about a specific user or current user
 * @summary Get User Details
 */
export const GetUserParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format), username, email, or \"me\" for current user'),
});

export const getUserHeaderXLangDefault = `es`;

export const GetUserHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(getUserHeaderXLangDefault).describe('Language code'),
});

export const getUserResponseOneUsernameMin = 3;
export const getUserResponseOneUsernameMax = 36;

export const getUserResponseOneDisplayNameMin = 3;
export const getUserResponseOneDisplayNameMax = 50;

export const getUserResponseTwoUsernameMin = 3;
export const getUserResponseTwoUsernameMax = 36;

export const getUserResponseTwoDisplayNameMin = 3;
export const getUserResponseTwoDisplayNameMax = 50;

export const GetUserResponse = zod.union([
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(getUserResponseOneUsernameMin)
        .max(getUserResponseOneUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(getUserResponseOneDisplayNameMin)
        .max(getUserResponseOneDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
    })
    .describe('Publicly visible user profile information shown to other users'),
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(getUserResponseTwoUsernameMin)
        .max(getUserResponseTwoUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(getUserResponseTwoDisplayNameMin)
        .max(getUserResponseTwoDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
      email: zod.string().describe('User email address (only visible to account owner)'),
      last_login_at: zod.iso
        .datetime({ offset: true })
        .nullable()
        .describe('ISO 8601 timestamp of the most recent login (only visible to account owner)'),
      mfa: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique MFA profile identifier (UUID format)'),
              enabled: zod.boolean().describe('Whether Multi-Factor Authentication is enabled for this account'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of MFA profile creation'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last MFA profile update'),
            })
            .describe('Represents user MFA security configuration and recovery credentials'),
          zod.null(),
        ])
        .describe('Multi-Factor Authentication profile linked to this user account'),
      updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last profile update (only visible to account owner)'),
    })
    .describe('Complete profile information for the authenticated user (private fields included)'),
]);

/**
 * Modifies user profile information (username and display name)
 * @summary Patch User Profile
 */
export const PatchUserParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format), username, email, or \"me\" for current user'),
});

export const patchUserHeaderXLangDefault = `es`;

export const PatchUserHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(patchUserHeaderXLangDefault).describe('Language code'),
});

export const patchUserBodyUsernameMin = 3;
export const patchUserBodyUsernameMax = 36;

export const patchUserBodyUsernameRegExp = new RegExp('^[a-z0-9]+$');
export const patchUserBodyDisplayNameMin = 3;
export const patchUserBodyDisplayNameMax = 50;

export const PatchUserBody = zod
  .object({
    username: zod
      .string()
      .min(patchUserBodyUsernameMin)
      .max(patchUserBodyUsernameMax)
      .regex(patchUserBodyUsernameRegExp)
      .optional()
      .describe('New unique username for login'),
    display_name: zod
      .string()
      .min(patchUserBodyDisplayNameMin)
      .max(patchUserBodyDisplayNameMax)
      .optional()
      .describe('New public display name shown to other users'),
  })
  .describe('User profile fields to update');

export const patchUserResponseOneUsernameMin = 3;
export const patchUserResponseOneUsernameMax = 36;

export const patchUserResponseOneDisplayNameMin = 3;
export const patchUserResponseOneDisplayNameMax = 50;

export const patchUserResponseTwoUsernameMin = 3;
export const patchUserResponseTwoUsernameMax = 36;

export const patchUserResponseTwoDisplayNameMin = 3;
export const patchUserResponseTwoDisplayNameMax = 50;

export const PatchUserResponse = zod.union([
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(patchUserResponseOneUsernameMin)
        .max(patchUserResponseOneUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(patchUserResponseOneDisplayNameMin)
        .max(patchUserResponseOneDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
    })
    .describe('Publicly visible user profile information shown to other users'),
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(patchUserResponseTwoUsernameMin)
        .max(patchUserResponseTwoUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(patchUserResponseTwoDisplayNameMin)
        .max(patchUserResponseTwoDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
      email: zod.string().describe('User email address (only visible to account owner)'),
      last_login_at: zod.iso
        .datetime({ offset: true })
        .nullable()
        .describe('ISO 8601 timestamp of the most recent login (only visible to account owner)'),
      mfa: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique MFA profile identifier (UUID format)'),
              enabled: zod.boolean().describe('Whether Multi-Factor Authentication is enabled for this account'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of MFA profile creation'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last MFA profile update'),
            })
            .describe('Represents user MFA security configuration and recovery credentials'),
          zod.null(),
        ])
        .describe('Multi-Factor Authentication profile linked to this user account'),
      updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last profile update (only visible to account owner)'),
    })
    .describe('Complete profile information for the authenticated user (private fields included)'),
]);

/**
 * Soft deletes a user account (marks it as deleted without removing it from the database)
 * @summary Delete User Account
 */
export const DeleteUserParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format), username, email, or \"me\" for current user'),
});

export const deleteUserHeaderXLangDefault = `es`;

export const DeleteUserHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(deleteUserHeaderXLangDefault).describe('Language code'),
});

export const deleteUserResponseOneUsernameMin = 3;
export const deleteUserResponseOneUsernameMax = 36;

export const deleteUserResponseOneDisplayNameMin = 3;
export const deleteUserResponseOneDisplayNameMax = 50;

export const deleteUserResponseTwoUsernameMin = 3;
export const deleteUserResponseTwoUsernameMax = 36;

export const deleteUserResponseTwoDisplayNameMin = 3;
export const deleteUserResponseTwoDisplayNameMax = 50;

export const DeleteUserResponse = zod.union([
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(deleteUserResponseOneUsernameMin)
        .max(deleteUserResponseOneUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(deleteUserResponseOneDisplayNameMin)
        .max(deleteUserResponseOneDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
    })
    .describe('Publicly visible user profile information shown to other users'),
  zod
    .object({
      id: zod.string().describe('Unique user identifier (UUID format)'),
      username: zod
        .string()
        .min(deleteUserResponseTwoUsernameMin)
        .max(deleteUserResponseTwoUsernameMax)
        .describe('Unique username for login and identification'),
      display_name: zod
        .string()
        .min(deleteUserResponseTwoDisplayNameMin)
        .max(deleteUserResponseTwoDisplayNameMax)
        .nullable()
        .describe('Public name displayed for other users'),
      is_email_verified: zod.boolean().describe('Whether the user has verified their email address'),
      roles: zod
        .array(
          zod
            .object({
              id: zod.string().describe('Unique role identifier (UUID format)'),
              name: zod.enum(['developer', 'admin', 'user']).describe('Role name for permission and access level identification'),
            })
            .describe('Represents a role or permission level assigned to a user'),
        )
        .describe('List of roles and permission levels assigned to the user'),
      picture: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique picture identifier (UUID format)'),
              url: zod.string().nullable().describe('Publicly accessible URL of the user profile image'),
              job_id: zod.string().nullable().describe('Reference ID for image processing job used for asynchronous tracking'),
              processing: zod.boolean().describe('Indicates whether the picture is still being processed'),
              origin: zod.enum(['local', 'external']).describe('Source of the picture (oauth provider, local upload, etc.)'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of picture upload'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last picture update'),
            })
            .describe('Represents a user profile picture or avatar image'),
          zod.null(),
        ])
        .describe('User profile picture or avatar information'),
      created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of account creation'),
      email: zod.string().describe('User email address (only visible to account owner)'),
      last_login_at: zod.iso
        .datetime({ offset: true })
        .nullable()
        .describe('ISO 8601 timestamp of the most recent login (only visible to account owner)'),
      mfa: zod
        .union([
          zod
            .object({
              id: zod.string().describe('Unique MFA profile identifier (UUID format)'),
              enabled: zod.boolean().describe('Whether Multi-Factor Authentication is enabled for this account'),
              created_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of MFA profile creation'),
              updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last MFA profile update'),
            })
            .describe('Represents user MFA security configuration and recovery credentials'),
          zod.null(),
        ])
        .describe('Multi-Factor Authentication profile linked to this user account'),
      updated_at: zod.iso.datetime({ offset: true }).describe('ISO 8601 timestamp of last profile update (only visible to account owner)'),
    })
    .describe('Complete profile information for the authenticated user (private fields included)'),
]);

/**
 * Uploads or updates the user profile picture
 * @summary Upload Profile Picture
 */
export const UpdateUserPictureParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format), username, email, or \"me\" for current user'),
});

export const updateUserPictureHeaderXLangDefault = `es`;

export const UpdateUserPictureHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(updateUserPictureHeaderXLangDefault).describe('Language code'),
});

export const UpdateUserPictureBody = zod
  .object({
    picture: zod.instanceof(File),
  })
  .describe('Image file for the profile picture');

export const UpdateUserPictureResponse = zod
  .object({
    job_id: zod.string().describe('Background processing job identifier from queue system for tracking'),
  })
  .describe('Background job details for image processing');

/**
 * Removes the user profile picture
 * @summary Delete Profile Picture
 */
export const DeleteUserPictureParams = zod.object({
  identifier: zod.string().describe('User ID (UUID format), username, email, or \"me\" for current user'),
});

export const deleteUserPictureHeaderXLangDefault = `es`;

export const DeleteUserPictureHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(deleteUserPictureHeaderXLangDefault).describe('Language code'),
});

export const DeleteUserPictureResponse = zod
  .object({
    job_id: zod.string().nullable().describe('Background processing job identifier from queue system for tracking'),
  })
  .describe('Confirmation and background job details');

/**
 * Restores a previously deleted user account using a recovery token
 * @summary Restore Deleted Account
 */
export const recoverAccountHeaderXLangDefault = `es`;

export const RecoverAccountHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(recoverAccountHeaderXLangDefault).describe('Language code'),
});

export const RecoverAccountBody = zod
  .object({
    token: zod.string().describe('Account recovery token from email link'),
  })
  .describe('Recovery token sent to the user email');

export const RecoverAccountResponse = zod.void();

/**
 * Sends account recovery instructions to a deleted user account
 * @summary Send Account Recovery Email
 */
export const sendRecoveryEmailHeaderXLangDefault = `es`;

export const SendRecoveryEmailHeader = zod.object({
  'x-lang': zod.enum(['en', 'es']).default(sendRecoveryEmailHeaderXLangDefault).describe('Language code'),
});

export const SendRecoveryEmailBody = zod
  .object({
    identifier: zod.string().describe('User ID (UUID format), username, or email address'),
  })
  .describe('Identifier used to locate the deleted account');

export const SendRecoveryEmailResponse = zod.void();
